{
  "name": "loanapp-functions",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,hosting,storage",
    "deploy": "firebase deploy --only functions,hosting,firestore:rules,storage:rules"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1"
  }
}
