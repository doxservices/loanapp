{
  "name": "loanapp",
  "version": "1.0.0",
  "private": true,
  "description": "Loanapp — retired local Express app; the live app is now Firebase Hosting + Cloud Functions (see functions/). This root package.json only backs one-off admin scripts in scripts/.",
  "scripts": {
    "seed": "node scripts/seed-firestore.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  }
}
